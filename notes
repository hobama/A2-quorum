Add lib path to Makefile.include

Define initiator and node count ( IF static)

Have a variable into Quorum_rund_begin to define type of operation (read-Write)

Rounds Modulo node count for deciding who's turn it is to perform an operation

Quorum_round_begin(round_count,id, operation, &value, &tag, &flags)

git checkout -b "branch_name" -- make new branch

git checkout "name" --  change branch